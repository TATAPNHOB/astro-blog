---
const { name } = Astro.props;

// const pages = await Astro.glob('../pages/*.astro');

const nav = [
    {
        "name": "Home",
        "url": "/"
    },
    {
        "name": "About",
        "url": "/about"
    },
    {
        "name": "Blog",
        "url": "/blog"
    },
    {
        "name": "Projects",
        "url": "/projects"
    },
]

---

<div class="nav-links">
    {nav.map((page) => (
        <a href={page.url} class={name != undefined && name.toLowerCase() == page.name.toLowerCase() ? "highlighted" : ""}>{page.name}</a>
    ))}
</div>